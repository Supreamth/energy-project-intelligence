Continue Energy Project Intelligence on this machine. Chat with the user in Thai.

Read first, in this order, and treat them as source of truth:
1. /opt/data/docs/energy-project-intelligence/handoff.md
2. /opt/data/docs/energy-project-intelligence/status.json
3. https://enerise.sprees.net/ and https://enerise.sprees.net/schema-v0.2.html
4. Git working copy: /opt/data/workspace/energy-project-intelligence

Do not re-litigate MVP scope. Do not reopen schema v0.2 decisions unless the user explicitly asks.

Done:
- Live docs at https://enerise.sprees.net/ with HTTP Basic Auth
- GitHub origin/main connected
- Schema v0.2 closed and migrated on PostgreSQL 18.6 + PostGIS 3.6 (127.0.0.1:55432)
- Manual ingestion CLI: `python -m energy_intelligence import fixtures/demo_phase_a.json`
- Proven: same snapshot does not add raw (adds fetch event); file/value change creates new raw and keeps old evidence; unknown predicate rejected; evidence stays pending
- pytest 10 passed

Next task: Review UI + canonical selection.
- Queue of pending evidence
- Accept/reject with append-only review events
- Select canonical with reason; 40 vs 50 MW can coexist
- Pending/rejected cannot become canonical
- Do not start collectors, satellite imagery, Solar Portfolio, public API, or LLM-approved facts

Hard rules:
- After EVERY status change, update https://enerise.sprees.net/ including the Next prompt, then sync docs into git and push
- Never commit secrets, auth.json, tokens, .env, var/, .conda/, .venv/
- Ingestion must not write canonical_selections
- Follow schema v0.2 exactly
