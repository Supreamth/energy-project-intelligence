Continue Energy Project Intelligence on this machine. Chat with the user in Thai.

Read first, in this order, and treat them as source of truth:
1. /opt/data/docs/energy-project-intelligence/handoff.md
2. /opt/data/docs/energy-project-intelligence/status.json
3. https://enerise.sprees.net/ and https://enerise.sprees.net/schema-v0.2.html
4. Git working copy: /opt/data/workspace/energy-project-intelligence

Do not re-litigate MVP scope. Do not reopen schema v0.2 decisions unless the user explicitly asks.

Done:
- Live docs at https://enerise.sprees.net/ with HTTP Basic Auth
- GitHub origin/main connected
- Schema v0.2 contract closed
- Foundation: Alembic migration applied on real PostgreSQL 18.6 + PostGIS 3.6 (user-space, port 55432). Roles epi_migration / epi_ingestion / epi_review / epi_product_read exist. Local object store module exists. pytest schema constraints 6 passed. Docker daemon is still unavailable; docker-compose.yml is for later.

Next task: Manual ingestion end-to-end on THIS host.
- CLI import of a licensed or demo fixture into raw object store + raw_records + fetch_events + pending evidence
- Prove idempotency: same snapshot does not insert a new raw row but does insert a fetch event
- Prove file change creates a new snapshot and keeps old evidence
- Do not start collectors, satellite imagery, Solar Portfolio, public API, or LLM-approved facts

Hard rules:
- After EVERY status change, update https://enerise.sprees.net/ including the Next prompt section, then sync docs into the git repo and push.
- Never commit secrets, auth.json, GitHub tokens, passwords, .env, var/, or .conda/
- NULL means unknown; never store 0 for missing
- Follow schema v0.2 exactly
- Ingestion role must not write canonical_selections
